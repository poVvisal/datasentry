# Copy to .env (never commit .env)
# These values are used by application.yml via ${VAR:default} syntax.

DB_URL=jdbc:postgresql://localhost:5432/datasentry
DB_USER=datasentry
DB_PASS=change_me_locally

# Active profile – switch to 'local' for local dev
SPRING_PROFILES_ACTIVE=local
